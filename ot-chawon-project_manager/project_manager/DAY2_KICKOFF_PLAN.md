# Day2 Kickoff Plan

## Sprint 1 Day2 Goal
- 계약(OpenAPI) 기반으로 최소 연동 경로를 만든다.
- API client, QA smoke 확장, AI endpoint 테스트, DevOps 실행 가이드를 정리한다.

## Work Order
1. backend: contract examples + error model
2. frontend: gateway API client + products fetch helper
3. ai: endpoint unit tests
4. devops: compose runbook + healthcheck flow
5. qa: smoke collection 확장 (health + size predict + invalid payload)
6. 3d: asset manifest + naming validation usage guide
7. pm: 수집/판정

## Rules
- commit 자율, push 승인제 유지
- Slack/Jira 호출 금지 유지
